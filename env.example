# Configuration Certificate Manager
# Copiez ce fichier vers .env et modifiez les valeurs selon vos besoins

# Répertoire des certificats à surveiller
CERTS_DIR=/data/certs

# Configuration serveur
PORT=8080
HOST=0.0.0.0

# Configuration du scan
SCAN_INTERVAL_SEC=300
THRESHOLD_DAYS=30

# Clé secrète pour JWT (générez une nouvelle clé en production)
SECRET_KEY=change-this-in-production

# Authentification (décommentez et configurez selon vos besoins)

# Option 1: Token d'authentification
# AUTH_TOKEN=your-secret-token-here

# Option 2: Basic Authentication
# BASIC_USER=admin
# BASIC_PASS=your-secure-password

# Configuration de la base de données (optionnel)
# DATABASE_URL=sqlite:///./certificates.db
